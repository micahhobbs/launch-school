/*
** In the code shown below, identify the following items:
** - the function arguments = 2, 3, 4 on line 18 function arguments
** - the function body = lines 14 and 15
** - the function declaration = the function is declared on line 13 through 16
** - the function invocation = the function is invoked on line 18
** - the function name = multiplyNumbers
** - the function parameters = num1, num2, num3
** - the function return value = the return value is 24, and is saved in the result variable
** - the names of all variables in this program = multiplyNumbers, num1|2|3, result, product
*/

function multiplyNumbers(num1, num2, num3) {
  let result = num1 * num2 * num3;
  return result;
}

let product = multiplyNumbers(2, 3, 4);
